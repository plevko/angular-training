import { Routes } from '@angular/router';
import { ContactFormComponent } from './contact-form.component';

export const contactFormRoutes: Routes = [
  {
    path: '',
    component: ContactFormComponent,
  },
];
